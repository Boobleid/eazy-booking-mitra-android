<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners
        android:radius="@dimen/space_50" />
    <stroke
        android:width="0.1dp"
        android:color="@color/colorNavy" />
    <solid android:color="@color/colorRed" />
</shape>
